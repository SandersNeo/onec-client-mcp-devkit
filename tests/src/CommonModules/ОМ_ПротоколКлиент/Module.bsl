#Область ПрограммныйИнтерфейс

// Регистрирует сценарии тестового модуля.
Процедура ИсполняемыеСценарии() Экспорт
	ЮТТесты
		.ДобавитьТест("НовыйОписаниеИнструментаЗаполняетПоля")
		.ДобавитьТест("НовыйОписаниеИнструментаДляАсинхронногоИнструментаДобавляетExecution")
		.ДобавитьТест("НовыйВызовИнструментаЗаполняетКонтракт")
		.ДобавитьТест("НовыйЗапросВызоваЗаполняетКонтракт")
		.ДобавитьТест("НовыйПараметрыЗавершенияЗадачиЗаполняетКонтракт")
		.ДобавитьТест("НовыйОписаниеРесурсаЗаполняетПоля")
		.ДобавитьТест("НовыйОписаниеШаблонаРесурсаЗаполняетПоля")
		.ДобавитьТест("НовыйОписаниеПромптаЗаполняетПоля")
		.ДобавитьТест("ПодготовитьЗавершениеЗадачиИнструментаДляУспехаВозвращаетResult")
		.ДобавитьТест("ПодготовитьЗавершениеЗадачиИнструментаНеСчитаетБизнесСтруктуруОшибкой")
		.ДобавитьТест("ПодготовитьЗавершениеЗадачиИнструментаДляОшибкиВозвращаетError")
		.ДобавитьТест("ПодготовитьЗавершениеЗадачиИнструментаДляЯвнойОшибкиВозвращаетError")
		.ДобавитьТест("ПодготовитьОтветЗапросаИнструментаДляУспехаВозвращаетResult")
		.ДобавитьТест("ПодготовитьОтветЗапросаИнструментаДляОшибкиВозвращаетError")
		.ДобавитьТест("ВызоватьАсинхронныйИнструментВСинхронномРежимеВозвращаетОтвет")
		.ДобавитьТест("ВызоватьАсинхронныйИнструментВСинхронномРежимеБезРезультатаВозвращаетНеопределено")
		.ДобавитьТест("ВызоватьАсинхронныйИнструментВСинхронномРежимеОшибкаИнструментаВозвращаетОшибку")
		.ДобавитьТест("ВызоватьАсинхронныйИнструментВРежимеЗадачиПередаетИдентификаторЗадачи")
		.ДобавитьТест("ВызватьИнструментВTaskРежимеБезTaskIdВозвращаетВнутреннююОшибку")
		.ДобавитьТест("ВызватьИнструментСНедопустимымРежимомВозвращаетВнутреннююОшибку")
		.ДобавитьТест("ВызватьTaskOnlyИнструментБезTaskМаркеровВозвращаетОшибку")
		.ДобавитьТест("ВызватьИнструментV2РежимИмеетПриоритетНадАсинхронностью")
		.ДобавитьТест("ВызватьИнструментV2SyncРежимИмеетПриоритетНадАсинхронностью")
		.ДобавитьТест("ВызоватьИнструментБезИмениВозвращаетОшибку")
		.ДобавитьТест("ВызоватьИнструментНезарегистрированныйВозвращаетОшибку")
		.ДобавитьТест("ВызоватьИнструментВозвращаетОтвет")
		.ДобавитьТест("ВызоватьИнструментВозвращаетОтветСКонтентом")
		.ДобавитьТест("ПрочитатьРесурсБезURIВозвращаетОшибку")
		.ДобавитьТест("ПрочитатьРесурсНезарегистрированныйВозвращаетОшибку")
		.ДобавитьТест("ПрочитатьРесурсВозвращаетОтвет")
		.ДобавитьТест("ПолучитьПромптБезИмениВозвращаетОшибку")
		.ДобавитьТест("ПолучитьПромптНезарегистрированныйВозвращаетОшибку")
		.ДобавитьТест("ПолучитьПромптВозвращаетОтвет")
		.ДобавитьТест("ЧтениеРесурсаПоШаблонуПередаетКонтекст")
		.ДобавитьТест("ТочныйРесурсИмеетПриоритетНадШаблоном")
		.ДобавитьТест("ЧтениеРесурсаФормыСНекорректнымURIВозвращает32602")
		.ДобавитьТест("ЧтениеРесурсаФормыБезКлиентаВозвращаетUI_NOT_READY");
КонецПроцедуры

// Проверяет, что НовыйОписаниеИнструмента заполняет поля name, description, inputSchema.
Процедура НовыйОписаниеИнструментаЗаполняетПоля() Экспорт
	Схема = Новый Структура("type", "object");

	Описание = Мсп_ПротоколКлиент.НовыйОписаниеИнструмента("мой_инструмент", "Описание", Схема);

	ЮТест.ОжидаетЧто(Описание)
		.Свойство("name").Равно("мой_инструмент")
		.Свойство("description").Равно("Описание")
		.Свойство("inputSchema").Равно(Схема);
КонецПроцедуры

// Проверяет, что асинхронный инструмент публикует execution.taskSupport.
Процедура НовыйОписаниеИнструментаДляАсинхронногоИнструментаДобавляетExecution() Экспорт
	Схема = Новый Структура("type", "object");

	Описание = Мсп_ПротоколКлиент.НовыйОписаниеИнструмента("мой_инструмент", "Описание", Схема, Истина);

	ЮТест.ОжидаетЧто(Описание.Свойство("execution")).Равно(Истина);
	ЮТест.ОжидаетЧто(Описание.execution.taskSupport).Равно("optional");
КонецПроцедуры

// Проверяет единый служебный контракт вызова инструмента.
Процедура НовыйВызовИнструментаЗаполняетКонтракт() Экспорт
	ВызовИнструмента = Мсп_ПротоколКлиент.НовыйВызовИнструмента(
		"request-1",
		"async_tool",
		Новый Структура("ticks", 3),
		"test-session",
		"task",
		"task-123",
		"progress-123");

	ЮТест.ОжидаетЧто(ВызовИнструмента)
		.Свойство("Идентификатор").Равно("request-1")
		.Свойство("ИмяИнструмента").Равно("async_tool")
		.Свойство("Сессия").Равно("test-session")
		.Свойство("РежимВыполнения").Равно("task")
		.Свойство("ИдентификаторЗадачи").Равно("task-123")
		.Свойство("ProgressToken").Равно("progress-123");
	ЮТест.ОжидаетЧто(ВызовИнструмента.Аргументы.ticks).Равно(3);
КонецПроцедуры

// Проверяет, что внутренний запрос вызова создается через единый контракт.
Процедура НовыйЗапросВызоваЗаполняетКонтракт() Экспорт
	Аргументы = Новый Структура("ticks", 3);
	Контекст = Новый Структура("kind,requestId", "tools", "request-1");

	Запрос = Мсп_ПротоколКлиент.НовыйЗапросВызова(Аргументы, Контекст, "task-123");

	ЮТест.ОжидаетЧто(Запрос)
		.Свойство("Аргументы").Равно(Аргументы)
		.Свойство("Контекст").Равно(Контекст)
		.Свойство("Идентификатор").Равно("request-1")
		.Свойство("ИдентификаторЗадачи").Равно("task-123");
КонецПроцедуры

// Проверяет, что параметры завершения задачи создаются через единый контракт.
Процедура НовыйПараметрыЗавершенияЗадачиЗаполняетКонтракт() Экспорт
	Тело = Новый Структура("result", Новый Структура("content", Новый Массив));

	ПараметрыЗавершения = Мсп_ПротоколКлиент.НовыйПараметрыЗавершенияЗадачи(
		"completed",
		"",
		Тело);

	ЮТест.ОжидаетЧто(ПараметрыЗавершения)
		.Свойство("Статус").Равно("completed")
		.Свойство("Сообщение").Равно("")
		.Свойство("Тело").Равно(Тело);
КонецПроцедуры

// Проверяет, что успешный результат задачи нормализуется в result.
Процедура ПодготовитьЗавершениеЗадачиИнструментаДляУспехаВозвращаетResult() Экспорт
	Подготовлено = Мсп_ПротоколКлиент.ПодготовитьЗавершениеЗадачиИнструмента("готово");

	ЮТест.ОжидаетЧто(Подготовлено)
		.Свойство("Статус").Равно("completed");
	ЮТест.ОжидаетЧто(Подготовлено.Тело.result.content[0].text).Равно("готово");
КонецПроцедуры

// Проверяет, что бизнес-структура с полем error не воспринимается как envelope ошибки.
Процедура ПодготовитьЗавершениеЗадачиИнструментаНеСчитаетБизнесСтруктуруОшибкой() Экспорт
	Подготовлено = Мсп_ПротоколКлиент.ПодготовитьЗавершениеЗадачиИнструмента(
		Новый Структура("error,count", Ложь, 3));

	ЮТест.ОжидаетЧто(Подготовлено)
		.Свойство("Статус").Равно("completed");
	ЮТест.ОжидаетЧто(Подготовлено.Тело.result.content[0].text).НеРавно("");
КонецПроцедуры

// Проверяет, что ошибка инструмента нормализуется в error.
Процедура ПодготовитьЗавершениеЗадачиИнструментаДляОшибкиВозвращаетError() Экспорт
	ОшибкаИнструмента = Мсп_Сервер.ОшибкаИнструмента("ACTION_FAILED", "что-то пошло не так");
	Подготовлено = Мсп_ПротоколКлиент.ПодготовитьЗавершениеЗадачиИнструмента(ОшибкаИнструмента);

	ЮТест.ОжидаетЧто(Подготовлено)
		.Свойство("Статус").Равно("failed");
	ЮТест.ОжидаетЧто(Подготовлено.Тело.error.code).Равно(-32603);
	ЮТест.ОжидаетЧто(Подготовлено.Тело.error.message).Равно("ACTION_FAILED: что-то пошло не так");
КонецПроцедуры

// Проверяет, что явный envelope ошибки задачи сохраняет failed-статус.
Процедура ПодготовитьЗавершениеЗадачиИнструментаДляЯвнойОшибкиВозвращаетError() Экспорт
	Подготовлено = Мсп_ПротоколКлиент.ПодготовитьЗавершениеЗадачиИнструмента(
		Новый Структура("error", Новый Структура("code,message", -32601, "Not found")));

	ЮТест.ОжидаетЧто(Подготовлено)
		.Свойство("Статус").Равно("failed");
	ЮТест.ОжидаетЧто(Подготовлено.Тело.error.code).Равно(-32601);
	ЮТест.ОжидаетЧто(Подготовлено.Тело.error.message).Равно("Not found");
КонецПроцедуры

// Проверяет формирование обычного JSON-RPC ответа по результату инструмента.
Процедура ПодготовитьОтветЗапросаИнструментаДляУспехаВозвращаетResult() Экспорт
	Ответ = Мсп_ПротоколКлиент.ПодготовитьОтветЗапросаИнструмента("request-1", "готово");

	ЮТест.ОжидаетЧто(Ответ)
		.Свойство("jsonrpc").Равно("2.0")
		.Свойство("id").Равно("request-1");
	ЮТест.ОжидаетЧто(Ответ.result.content[0].text).Равно("готово");
КонецПроцедуры

// Проверяет формирование обычной JSON-RPC ошибки по коду инструмента.
Процедура ПодготовитьОтветЗапросаИнструментаДляОшибкиВозвращаетError() Экспорт
	Ответ = Мсп_ПротоколКлиент.ПодготовитьОтветЗапросаИнструмента(
		"request-1",
		Мсп_Сервер.ОшибкаИнструмента("ACTION_FAILED", "что-то пошло не так"));

	ЮТест.ОжидаетЧто(Ответ)
		.Свойство("jsonrpc").Равно("2.0")
		.Свойство("id").Равно("request-1");
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32603);
	ЮТест.ОжидаетЧто(Ответ.error.message).Равно("ACTION_FAILED: что-то пошло не так");
КонецПроцедуры

// Проверяет, что асинхронный инструмент можно вызвать без task-режима.
Процедура ВызоватьАсинхронныйИнструментВСинхронномРежимеВозвращаетОтвет() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("async_tool", "ОбработчикАсинхронногоИнструментаСКороткимОтветом", Истина);

	Ответ = ВызватьИнструмент(1, Новый Структура("name", "async_tool"), "test-session");
	ЮТест.ОжидаетЧто(Ответ.jsonrpc).Равно("2.0");
	ЮТест.ОжидаетЧто(Ответ.result.content[0].text).Равно("async-result");
КонецПроцедуры

// Проверяет, что асинхронный инструмент может отложить обычный tools/call.
Процедура ВызоватьАсинхронныйИнструментВСинхронномРежимеБезРезультатаВозвращаетНеопределено() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("async_tool", "ОбработчикАсинхронногоИнструмента", Истина);

	Ответ = ВызватьИнструмент(1, Новый Структура("name", "async_tool"), "test-session");
	ПоследнийАсинхронныйЗапрос = ПолучитьПоследнийАсинхронныйЗапрос();

	ЮТест.ОжидаетЧто(Ответ).ЭтоНеопределено();
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос.Контекст)
		.Свойство("requestId").Равно(1)
		.Свойство("executionMode").Равно("sync");
КонецПроцедуры

// Проверяет, что sync-вызов async-инструмента маппит ОшибкаИнструмента в JSON-RPC ошибку.
Процедура ВызоватьАсинхронныйИнструментВСинхронномРежимеОшибкаИнструментаВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("async_tool", "ОбработчикАсинхронногоИнструментаСОшибкой", Истина);

	Ответ = ВызватьИнструмент(1, Новый Структура("name", "async_tool"), "test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32603)
		.Свойство("message").Равно("ACTION_FAILED: sync mode is not supported");
КонецПроцедуры

// Проверяет передачу идентификатора task-based задачи в асинхронный инструмент.
Процедура ВызоватьАсинхронныйИнструментВРежимеЗадачиПередаетИдентификаторЗадачи() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("async_tool", "ОбработчикАсинхронногоИнструмента", Истина);

	Параметры = Новый Структура(
		"name,arguments,executionMode,taskId,progressToken",
		"async_tool",
		Новый Структура("ticks", 3),
		"task",
		"task-123",
		"progress-123");
	Ответ = ВызватьИнструмент("", Параметры, "test-session");
	ПоследнийАсинхронныйЗапрос = ПолучитьПоследнийАсинхронныйЗапрос();

	ЮТест.ОжидаетЧто(Ответ).ЭтоНеопределено();
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос)
		.Свойство("ИдентификаторЗадачи").Равно("task-123");
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос.Контекст)
		.Свойство("executionMode").Равно("task")
		.Свойство("progressToken").Равно("progress-123")
		.Свойство("sessionId").Равно("test-session");
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос.Контекст).НетСвойства("responseMode");
	ЮТест.ОжидаетЧто(ПоследнийАсинхронныйЗапрос.Контекст.meta)
		.Свойство("progressToken").Равно("progress-123");
КонецПроцедуры

// Проверяет служебный контракт task-вызова: taskId обязателен.
Процедура ВызватьИнструментВTaskРежимеБезTaskIdВозвращаетВнутреннююОшибку() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("sync_tool", "ОбработчикИнструмента");

	Параметры = Новый Структура(
		"name,executionMode",
		"sync_tool",
		"task");
	Ответ = ВызватьИнструмент("request-1", Параметры, "test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32603)
		.Свойство("message").Равно("Internal error: Task result routing is invalid");
КонецПроцедуры

// Проверяет служебный контракт executionMode: transport должен передавать только допустимые значения.
Процедура ВызватьИнструментСНедопустимымРежимомВозвращаетВнутреннююОшибку() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("async_tool", "ОбработчикАсинхронногоИнструментаСКороткимОтветом", Истина);

	Параметры = Новый Структура(
		"name,executionMode",
		"async_tool",
		"async");
	Ответ = ВызватьИнструмент("request-1", Параметры, "test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32603)
		.Свойство("message").Равно("Internal error: Tool call execution mode is invalid");
	ЮТест.ОжидаетЧто(ПолучитьПоследнийАсинхронныйЗапрос()).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что task-only дескриптор не принимает обычный tools/call без task-маркеров.
Процедура ВызватьTaskOnlyИнструментБезTaskМаркеровВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструментV2("task_only_tool", "ОбработчикАсинхронногоИнструментаСКороткимОтветом", "task");

	Ответ = ВызватьИнструмент(
		"request-1",
		Новый Структура("name", "task_only_tool"),
		"test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32601)
		.Свойство("message").Равно("Tool requires task-based invocation");
	ЮТест.ОжидаетЧто(ПолучитьПоследнийАсинхронныйЗапрос()).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что v2 РежимВыполнения имеет приоритет над legacy Асинхронный.
Процедура ВызватьИнструментV2РежимИмеетПриоритетНадАсинхронностью() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("mixed_task_tool", "ОбработчикИнструмента", Ложь, "task");

	Параметры = Новый Структура(
		"name,arguments,executionMode,taskId",
		"mixed_task_tool",
		Новый Структура("ticks", 2),
		"task",
		"task-456");
	Ответ = ВызватьИнструмент("", Параметры, "test-session");

	ЮТест.ОжидаетЧто(Ответ.jsonrpc).Равно("2.0");
	ЮТест.ОжидаетЧто(Ответ.result.content[0].text).Равно("результат");
КонецПроцедуры

// Проверяет, что v2 sync-режим направляет task-вызов в завершение задачи, а не в async-обработчик.
Процедура ВызватьИнструментV2SyncРежимИмеетПриоритетНадАсинхронностью() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент(
		"mixed_sync_tool",
		"ОбработчикАсинхронногоИнструментаСКороткимОтветом",
		Истина,
		"sync");

	Параметры = Новый Структура(
		"name,executionMode,taskId",
		"mixed_sync_tool",
		"task",
		"task-789");
	Ответ = ВызватьИнструмент("request-1", Параметры, "test-session");

	ЮТест.ОжидаетЧто(Ответ).ЭтоНеопределено();
	ЮТест.ОжидаетЧто(ПолучитьПоследнийАсинхронныйЗапрос()).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что НовыйОписаниеРесурса заполняет поля uri, name, description, mimeType.
Процедура НовыйОписаниеРесурсаЗаполняетПоля() Экспорт
	Описание = Мсп_ПротоколКлиент.НовыйОписаниеРесурса("test://ресурс", "Ресурс", "Описание", "application/json");

	ЮТест.ОжидаетЧто(Описание)
		.Свойство("uri").Равно("test://ресурс")
		.Свойство("name").Равно("Ресурс")
		.Свойство("mimeType").Равно("application/json");
КонецПроцедуры

// Проверяет, что НовыйОписаниеШаблонаРесурса заполняет поля uriTemplate, name, description, mimeType.
Процедура НовыйОписаниеШаблонаРесурсаЗаполняетПоля() Экспорт
	Описание = Мсп_ПротоколКлиент.НовыйОписаниеШаблонаРесурса("test://ресурс/{id}", "Шаблон", "Описание", "application/json");

	ЮТест.ОжидаетЧто(Описание)
		.Свойство("uriTemplate").Равно("test://ресурс/{id}")
		.Свойство("name").Равно("Шаблон");
КонецПроцедуры

// Проверяет, что НовыйОписаниеПромпта заполняет поля name, description, arguments.
Процедура НовыйОписаниеПромптаЗаполняетПоля() Экспорт
	Аргументы = Новый Структура("arg1", "описание");

	Описание = Мсп_ПротоколКлиент.НовыйОписаниеПромпта("мой_промпт", "Описание", Аргументы);

	ЮТест.ОжидаетЧто(Описание)
		.Свойство("name").Равно("мой_промпт")
		.Свойство("description").Равно("Описание")
		.Свойство("arguments").Равно(Аргументы);
КонецПроцедуры

// Проверяет, что ВызоватьИнструмент без имени возвращает JSON-RPC ошибку -32602.
Процедура ВызоватьИнструментБезИмениВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();

	Ответ = ВызватьИнструмент(1, Новый Структура, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32602);
КонецПроцедуры

// Проверяет, что ВызоватьИнструмент с незарегистрированным именем возвращает ошибку -32601.
Процедура ВызоватьИнструментНезарегистрированныйВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();
	Параметры = Новый Структура("name", "несуществующий");

	Ответ = ВызватьИнструмент(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32601);
КонецПроцедуры

// Проверяет, что ВызоватьИнструмент с зарегистрированным обработчиком возвращает ответ с result.
Процедура ВызоватьИнструментВозвращаетОтвет() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("тест", "ОбработчикИнструмента");

	Параметры = Новый Структура("name", "тест");
	Ответ = ВызватьИнструмент(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.jsonrpc).Равно("2.0");
	ЮТест.ОжидаетЧто(Ответ.Свойство("result")).ЭтоИстина();
КонецПроцедуры

// Проверяет, что ВызоватьИнструмент нормализует строковый результат в content[0].text.
Процедура ВызоватьИнструментВозвращаетОтветСКонтентом() Экспорт
	ПодготовитьСостояние();
	ЗарегистрироватьТестовыйИнструмент("тест2", "ОбработчикИнструмента");

	Параметры = Новый Структура("name", "тест2");
	Ответ = ВызватьИнструмент(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.result.content[0].text).Равно("результат");
КонецПроцедуры

// Проверяет, что ПрочитатьРесурс без URI возвращает ошибку -32602.
Процедура ПрочитатьРесурсБезURIВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();

	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс(1, Новый Структура, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32602);
КонецПроцедуры

// Проверяет, что ПрочитатьРесурс с незарегистрированным URI возвращает ошибку -32601.
Процедура ПрочитатьРесурсНезарегистрированныйВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();
	Параметры = Новый Структура("uri", "test://нет");

	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32601);
КонецПроцедуры

// Проверяет, что ПрочитатьРесурс с зарегистрированным ресурсом возвращает ответ с result.
Процедура ПрочитатьРесурсВозвращаетОтвет() Экспорт
	ПодготовитьСостояние();
	Описание = Мсп_РеестрКлиент.НовыйОписаниеРесурса();
	Описание.URI = "test://ресурс";
	Описание.MimeType = "application/json";
	Описание.Обработчик = Новый ОписаниеОповещения("ОбработчикРесурса", ЭтотОбъект);
	Мсп_РеестрКлиент.ЗарегистрироватьРесурс(Описание);

	Параметры = Новый Структура("uri", "test://ресурс");
	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.jsonrpc).Равно("2.0");
	ЮТест.ОжидаетЧто(Ответ.Свойство("result")).ЭтоИстина();
КонецПроцедуры

// Проверяет, что ПолучитьПромпт без имени возвращает ошибку -32602.
Процедура ПолучитьПромптБезИмениВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();

	Ответ = Мсп_ПротоколКлиент.ПолучитьПромпт(1, Новый Структура, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32602);
КонецПроцедуры

// Проверяет, что ПолучитьПромпт с незарегистрированным именем возвращает ошибку -32601.
Процедура ПолучитьПромптНезарегистрированныйВозвращаетОшибку() Экспорт
	ПодготовитьСостояние();
	Параметры = Новый Структура("name", "несуществующий");

	Ответ = Мсп_ПротоколКлиент.ПолучитьПромпт(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.Свойство("error")).ЭтоИстина();
	ЮТест.ОжидаетЧто(Ответ.error.code).Равно(-32601);
КонецПроцедуры

// Проверяет передачу uriTemplate и arguments в обработчик template-ресурса.
Процедура ЧтениеРесурсаПоШаблонуПередаетКонтекст() Экспорт
	ПодготовитьПустыеРесурсы();
	Мсп_РеестрКлиент.ЗарегистрироватьШаблонРесурса(НовыйОписаниеШаблонаРесурсаСОбработчиком(
		"test://{id}",
		"Тестовый шаблон",
		"Проверка template-ресурса",
		Новый ОписаниеОповещения("ОбработатьТестовыйШаблонРесурса", ЭтотОбъект)));

	Параметры = Новый Структура("uri,uriTemplate,arguments", "test://42", "test://{id}", Новый Структура("id", "42"));
	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс("1", Параметры, "test-session");
	Контент = ПолучитьКонтентРесурса(Ответ);
	Данные = Мсп_СериализацияКлиент.ПрочитатьJSONСтроку(Мсп_Коллекции.Свойство(Контент, "text", ""));

	ЮТест.ОжидаетЧто(Контент)
		.Свойство("uri").Равно("test://42");
	ЮТест.ОжидаетЧто(Данные)
		.Свойство("uri").Равно("test://42")
		.Свойство("uriTemplate").Равно("test://{id}")
		.Свойство("argumentId").Равно("42");
КонецПроцедуры

// Проверяет приоритет точного ресурса над шаблоном при разрешении дескриптора.
Процедура ТочныйРесурсИмеетПриоритетНадШаблоном() Экспорт
	ПодготовитьПустыеРесурсы();
	Мсп_РеестрКлиент.ЗарегистрироватьРесурс(НовыйОписаниеРесурсаСОбработчиком(
		"test://42",
		"Точный ресурс",
		"Проверка приоритета точного ресурса",
		Новый ОписаниеОповещения("ОбработатьТочныйТестовыйРесурс", ЭтотОбъект)));
	Мсп_РеестрКлиент.ЗарегистрироватьШаблонРесурса(НовыйОписаниеШаблонаРесурсаСОбработчиком(
		"test://{id}",
		"Тестовый шаблон",
		"Проверка приоритета точного ресурса",
		Новый ОписаниеОповещения("ОбработатьТестовыйШаблонРесурса", ЭтотОбъект)));

	Параметры = Новый Структура("uri,uriTemplate,arguments", "test://42", "test://{id}", Новый Структура("id", "42"));
	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс("1", Параметры, "test-session");
	Контент = ПолучитьКонтентРесурса(Ответ);

	ЮТест.ОжидаетЧто(Мсп_Коллекции.Свойство(Контент, "text", "")).Равно("exact");
КонецПроцедуры

// Проверяет маппинг некорректного URI ресурса формы в -32602.
Процедура ЧтениеРесурсаФормыСНекорректнымURIВозвращает32602() Экспорт
	ПодготовитьРесурсыПровайдеров();

	Параметры = Новый Структура("uri,uriTemplate,arguments", "form://", "form://{name}", Новый Структура);
	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс("1", Параметры, "test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32602)
		.Свойство("message").Равно("INVALID_PARAMS: form uri segment is empty");
КонецПроцедуры

// Проверяет ошибку чтения формы без подключенного тест-клиента.
Процедура ЧтениеРесурсаФормыБезКлиентаВозвращаетUI_NOT_READY() Экспорт
	ПодготовитьРесурсыПровайдеров();
	ПодготовитьСостояниеБезТестКлиента();

	Параметры = Новый Структура(
		"uri,uriTemplate,arguments",
		"form://%D0%A4%D0%BE%D1%80%D0%BC%D0%B0",
		"form://{name}",
		Новый Структура("name", "Форма"));
	Ответ = Мсп_ПротоколКлиент.ПрочитатьРесурс("1", Параметры, "test-session");
	Ошибка = ПолучитьОшибкуОтвета(Ответ);

	ЮТест.ОжидаетЧто(Ошибка)
		.Свойство("code").Равно(-32000)
		.Свойство("message").Равно("NOT_READY: test client not connected");
КонецПроцедуры

// Проверяет, что ПолучитьПромпт с зарегистрированным промптом возвращает ответ с result.messages.
Процедура ПолучитьПромптВозвращаетОтвет() Экспорт
	ПодготовитьСостояние();
	Описание = Мсп_РеестрКлиент.НовыйОписаниеПромпта();
	Описание.Имя = "тест_промпт";
	Описание.Обработчик = Новый ОписаниеОповещения("ОбработчикПромпта", ЭтотОбъект);
	Мсп_РеестрКлиент.ЗарегистрироватьПромпт(Описание);

	Параметры = Новый Структура("name", "тест_промпт");
	Ответ = Мсп_ПротоколКлиент.ПолучитьПромпт(1, Параметры, "");

	ЮТест.ОжидаетЧто(Ответ.jsonrpc).Равно("2.0");
	ЮТест.ОжидаетЧто(Ответ.result.Свойство("messages")).ЭтоИстина();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПодготовитьСостояние()
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Инструменты", Новый Соответствие);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Ресурсы", Новый Соответствие);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ШаблоныРесурсов", Новый Соответствие);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Промпты", Новый Соответствие);
	СохранитьПоследнийАсинхронныйЗапрос(Неопределено);
КонецПроцедуры

Процедура ЗарегистрироватьТестовыйИнструмент(Имя, ИмяОбработчика, Асинхронный = Ложь, РежимВыполнения = Неопределено)
	Описание = Мсп_РеестрКлиент.НовыйОписаниеИнструмента();
	Описание.Имя = Имя;
	Описание.Обработчик = Новый ОписаниеОповещения(ИмяОбработчика, ЭтотОбъект);
	Описание.Асинхронный = Асинхронный;
	Если РежимВыполнения <> Неопределено Тогда
		Описание.Вставить("РежимВыполнения", РежимВыполнения);
	КонецЕсли;
	Мсп_РеестрКлиент.ЗарегистрироватьИнструмент(Описание);
КонецПроцедуры

Процедура ЗарегистрироватьТестовыйИнструментV2(Имя, ИмяОбработчика, РежимВыполнения)
	Описание = Мсп_Сервер.НовыйИнструмент(
		Имя,
		"Описание",
		Новый Структура("type", "object"),
		Новый ОписаниеОповещения(ИмяОбработчика, ЭтотОбъект),
		РежимВыполнения);
	Мсп_РеестрКлиент.ЗарегистрироватьИнструмент(Описание);
КонецПроцедуры

Функция ОбработчикИнструмента(Запрос, ДополнительныеПараметры) Экспорт
	Возврат "результат";
КонецФункции

Функция ОбработчикАсинхронногоИнструмента(Запрос, ДополнительныеПараметры) Экспорт
	СохранитьПоследнийАсинхронныйЗапрос(Запрос);
	Возврат Неопределено;
КонецФункции

Функция ОбработчикАсинхронногоИнструментаСКороткимОтветом(Запрос, ДополнительныеПараметры) Экспорт
	СохранитьПоследнийАсинхронныйЗапрос(Запрос);
	Возврат "async-result";
КонецФункции

Функция ОбработчикАсинхронногоИнструментаСОшибкой(Запрос, ДополнительныеПараметры) Экспорт
	СохранитьПоследнийАсинхронныйЗапрос(Запрос);
	Возврат Мсп_Сервер.ОшибкаИнструмента("ACTION_FAILED", "sync mode is not supported");
КонецФункции

Функция ОбработчикРесурса(Запрос, ДополнительныеПараметры) Экспорт
	Возврат Новый Структура("contents", Новый Массив);
КонецФункции

Функция ОбработчикПромпта(Запрос, ДополнительныеПараметры) Экспорт
	Возврат Новый Структура("messages", Новый Массив);
КонецФункции

Процедура ПодготовитьПустыеРесурсы()
	Мсп_РеестрКлиент.ИнициализироватьРеестры();
	Мсп_РеестрКлиент.РеестрИнструментов().Очистить();
	Мсп_РеестрКлиент.РеестрРесурсов().Очистить();
	Мсп_РеестрКлиент.РеестрШаблоновРесурсов().Очистить();
	Мсп_РеестрКлиент.РеестрПромптов().Очистить();
КонецПроцедуры

Процедура ПодготовитьРесурсыПровайдеров()
	ПодготовитьПустыеРесурсы();
	Мсп_ПровайдерыКлиент.ЗарегистрироватьПровайдеров();
КонецПроцедуры

Процедура ПодготовитьСостояниеБезТестКлиента()
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера(
		"ПараметрыЛогирования",
		Новый Структура("Логировать,УровеньЛога,ФайлЛога", Ложь, "error", ""));
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ПамятьТестКлиент", Новый Структура);
КонецПроцедуры

Функция НовыйОписаниеРесурсаСОбработчиком(URI, Имя, Описание, Обработчик)
	ОписаниеРесурса = Мсп_РеестрКлиент.НовыйОписаниеРесурса();
	ОписаниеРесурса.URI = URI;
	ОписаниеРесурса.Имя = Имя;
	ОписаниеРесурса.Описание = Описание;
	ОписаниеРесурса.MimeType = "application/json";
	ОписаниеРесурса.Обработчик = Обработчик;
	Возврат ОписаниеРесурса;
КонецФункции

Функция НовыйОписаниеШаблонаРесурсаСОбработчиком(URIШаблон, Имя, Описание, Обработчик)
	ОписаниеШаблонаРесурса = Мсп_РеестрКлиент.НовыйОписаниеШаблонаРесурса();
	ОписаниеШаблонаРесурса.URIШаблон = URIШаблон;
	ОписаниеШаблонаРесурса.Имя = Имя;
	ОписаниеШаблонаРесурса.Описание = Описание;
	ОписаниеШаблонаРесурса.MimeType = "application/json";
	ОписаниеШаблонаРесурса.Обработчик = Обработчик;
	Возврат ОписаниеШаблонаРесурса;
КонецФункции

Функция ПолучитьКонтентРесурса(Ответ)
	Если Ответ = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Результат = Мсп_Коллекции.Свойство(Ответ, "result", Неопределено);
	Если Результат = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Контент = Мсп_Коллекции.Свойство(Результат, "contents", Неопределено);
	Если ТипЗнч(Контент) <> Тип("Массив")
		ИЛИ Контент.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;

	Возврат Контент[0];
КонецФункции

Функция ПолучитьОшибкуОтвета(Ответ)
	Если Ответ = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Возврат Мсп_Коллекции.Свойство(Ответ, "error", Неопределено);
КонецФункции

Функция СоздатьВызовИнструмента(Идентификатор, Параметры, Сессия = "")
	Возврат Мсп_ПротоколКлиент.НовыйВызовИнструмента(
		Идентификатор,
		Мсп_Коллекции.Свойство(Параметры, "name", ""),
		Мсп_Коллекции.Свойство(Параметры, "arguments", Новый Структура),
		Сессия,
		Мсп_Коллекции.Свойство(Параметры, "executionMode", "sync"),
		Мсп_Коллекции.Свойство(Параметры, "taskId", ""),
		Мсп_Коллекции.Свойство(Параметры, "progressToken", Неопределено));
КонецФункции

Функция ВызватьИнструмент(Идентификатор, Параметры, Сессия = "")
	Возврат Мсп_ПротоколКлиент.ВызватьИнструмент(
		СоздатьВызовИнструмента(Идентификатор, Параметры, Сессия));
КонецФункции

Функция ОбработатьТестовыйШаблонРесурса(Запрос, ДополнительныеПараметры) Экспорт
	Аргументы = Мсп_Коллекции.Свойство(Запрос.Контекст, "arguments", Новый Структура);
	Данные = Новый Структура(
		"uri,uriTemplate,argumentId",
		Мсп_Коллекции.Свойство(Запрос.Контекст, "uri", ""),
		Мсп_Коллекции.Свойство(Запрос.Контекст, "uriTemplate", ""),
		Мсп_Коллекции.Свойство(Аргументы, "id", ""));
	Возврат Мсп_СериализацияКлиент.Сериализовать(Данные);
КонецФункции

Функция ОбработатьТочныйТестовыйРесурс(Запрос, ДополнительныеПараметры) Экспорт
	Возврат "exact";
КонецФункции

Процедура СохранитьПоследнийАсинхронныйЗапрос(Запрос)
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера(
		"ОМ_ПротоколКлиентПоследнийАсинхронныйЗапрос",
		Запрос);
КонецПроцедуры

Функция ПолучитьПоследнийАсинхронныйЗапрос()
	Возврат Мсп_СостояниеКлиент.ЗначениеСостоянияСервера(
		"ОМ_ПротоколКлиентПоследнийАсинхронныйЗапрос",
		Неопределено);
КонецФункции

#КонецОбласти
